# Ask the user for a number and print its square (the product of the number times itself).


num = float(input("Type a number to see its sqare:" ))

print(str(num) + "squared is " + str(num ** 2))

